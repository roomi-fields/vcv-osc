#pragma once
#include <rack.hpp>

using namespace rack;

// The plugin instance, defined in plugin.cpp.
extern Plugin* pluginInstance;

// Every module model exposed by this plugin.
extern Model* modelOscController;
